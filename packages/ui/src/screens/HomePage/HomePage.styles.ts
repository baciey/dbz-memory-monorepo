import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16,
    flexGrow: 1,
    minHeight: "100%",
    width: "100%",
    justifyContent: "center",
    alignItems: "center",
  },
  heading: {
    alignSelf: "center",
    marginBottom: 50,
  },
});
