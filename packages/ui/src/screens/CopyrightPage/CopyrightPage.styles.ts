import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    padding: 16,
  },
  cardTitle: {
    minHeight: 0,
    marginBottom: 8,
  },
  avatar: {
    backgroundColor: "white",
  },
});
