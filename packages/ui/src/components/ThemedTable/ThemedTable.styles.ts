import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    width: "100%",
  },
  scrollview: {
    flexGrow: 0,
  },
  noDataContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
});
