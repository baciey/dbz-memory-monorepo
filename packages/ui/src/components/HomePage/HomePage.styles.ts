import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16,
    paddingTop: 100,
    flex: 1,
    minHeight: "100%",
  },
  heading: {
    alignSelf: "center",
    marginBottom: 50,
  },
});
